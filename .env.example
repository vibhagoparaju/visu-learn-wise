# VISU environment variables (auto-managed by Lovable Cloud — do not edit .env directly)
# This file is a placeholder showing which variables are expected.
# Real values come from Lovable Cloud / Supabase project settings.

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-anon-key
VITE_SUPABASE_PROJECT_ID=your-project-ref

# Backend secrets (configured in Lovable Cloud → Backend → Secrets, NOT here):
# - GEMINI_API_KEY
# - ANTHROPIC_API_KEY
# - YOUTUBE_API_KEY
# - SUPABASE_SERVICE_ROLE_KEY (auto-provided to edge functions)

# TODO: manual step needed — rotate GEMINI_API_KEY and ANTHROPIC_API_KEY in their dashboards
# (Google AI Studio + Anthropic Console) since they may have been exposed previously.
